const state = {
    
}

const getters = {

}

const actions = {
	    // pushAllInfo({ dispatch, commit }, params) {
	    //                 commit("getReportInfo", res.data);
	    // },
}

const mutations = {
    getReportInfo(state, prop) {
        // state.reportInfo = prop;
    },
}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}